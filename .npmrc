# Skip optional dependencies that fail due to platform mismatches
# This prevents build failures from platform-specific packages
# like oh-my-opencode-darwin-arm64 on Linux CI/CD environments
optionalDependencies=false
